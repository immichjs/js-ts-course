const frutas = ['pera', 'maçã', 'uva']
for (let i in frutas) {
  console.log(frutas[i])
}

const pessoa = {
  nome: 'Mich',
  sobrenome: 'França',
  idade: 19
}

for (let key in pessoa) {
  console.log(pessoa[key])
}

